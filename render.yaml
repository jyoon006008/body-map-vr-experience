services:
  - type: web
    name: body-map-vr-experience
    env: node
    rootDir: web
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MESHY_API_KEY
        sync: false
