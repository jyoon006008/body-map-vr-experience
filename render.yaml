services:
  - type: web
    name: jemmy-api
    env: python
    rootDir: web
    buildCommand: pip install -r requirements.txt
    startCommand: python jemmy_api_server.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MESHY_API_KEY
        sync: false
